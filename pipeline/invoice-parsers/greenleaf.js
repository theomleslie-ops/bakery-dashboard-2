// Greenleaf Produce invoice PDF parser.
// PLACEHOLDER: Real parser will be written after inspecting a sample Greenleaf invoice PDF.
// Expected format: each vendor's PDF layout is unique; this will anchor on their specific line markers.

const parseInvoiceText = (text) => {
  // TODO: Write parser by inspecting sample Greenleaf invoice PDF
  // For now, return empty array to prevent crashes during development
  return [];
};

module.exports = {
  parseInvoiceText,
};
