// Alan Brothers invoice PDF parser.
// PLACEHOLDER: Real parser will be written after inspecting a sample Alan Brothers invoice PDF.
// Expected format: each vendor's PDF layout is unique; this will anchor on their specific line markers.

const parseInvoiceText = (text) => {
  // TODO: Write parser by inspecting sample Alan Brothers invoice PDF
  // For now, return empty array to prevent crashes during development
  return [];
};

module.exports = {
  parseInvoiceText,
};
